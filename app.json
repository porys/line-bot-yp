{
    "name": "LINE Messaging API Bot",
    "description": "This is simple LINE Messaging API callback bot to get Ids and watch requests",
    "keywords": ["chatbot", "line", "node"],
    "env": {
        "LINE_TOKEN": {
            "description": "LINE bot Channel access token from the Channel Console",
            "required":  true
        },
        "LINE_SECRET": {
            "description": "LINE bot Channel secret from the Channel Console",
            "required":  true
        }
    }
}
